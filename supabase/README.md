# Legacy Supabase Files

The project delivery target is now self-hosted PostgreSQL with Prisma and Docker.

Files in this directory are kept only as an old schema reference during migration and should not be used for production deployment.

Use:

```txt
prisma/schema.prisma
docker-compose.yml
.env.docker.example
```

for the self-host version.
